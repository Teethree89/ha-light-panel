{
  "name": "ha-light-panel",
  "version": "0.2.0",
  "private": true,
  "description": "A tiny Home Assistant panel server for low-power browsers, tablets, and digital frames.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "validate": "node scripts/validate-config.js && node scripts/validate-addon-repo.js",
    "test": "node scripts/validate-config.js examples/frameo-climate.json && node scripts/validate-config.js examples/starter.json && node scripts/validate-addon-repo.js && node --check server.js",
    "demo": "node scripts/demo-server.js",
    "demo:ha": "node scripts/demo-server.js --ha-only",
    "sync-addon": "node scripts/sync-addon.js"
  },
  "engines": {
    "node": ">=20"
  }
}

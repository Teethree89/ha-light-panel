services:
  ha-light-panel:
    build: .
    container_name: ha-light-panel
    restart: unless-stopped
    ports:
      - "8890:8890"
    environment:
      HA_URL: "${HA_URL}"
      HA_BROWSER_URL: "${HA_BROWSER_URL}"
      HA_TOKEN: "${HA_TOKEN}"
      CONFIG_PATH: "/app/config.json"
    volumes:
      - ./config.json:/app/config.json:ro

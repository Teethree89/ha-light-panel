# The panel service runs with ProtectSystem=strict, so it cannot write the
# spool files the .path units watch without these exceptions.
[Service]
ReadWritePaths=/var/spool/blink-reauth
ReadWritePaths=/var/spool/blink-liveview-proxy-restart
